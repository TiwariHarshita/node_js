const nm1=5;
const nm2=10;

function addValues(){
    console.log(`the sum is${nm1+nm2}`);
    
}

addValues(); // if we a func inside a module that is envoked then it will run even if we dont set it up to a variable in the require statement.